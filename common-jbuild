(executables
 ((names     (a b))
  (libraries (core))
  (preprocess (pps (ppx_jane -annotated-ignores ppx_driver.runner)))))

(alias
 ((name runtest)
  (deps (input))
  (action (progn (run ${exe:a.exe} input) (run ${exe:b.exe} input)))))
